import { useNavigate } from "react-router-dom";

export function History() {
    const navigate = useNavigate();

    return (
        <div>
            <h1>HISTORY</h1>
            
        </div>
    );
}
